import 'package:flutter/material.dart';

class PlayerPlaylistPage extends StatelessWidget {
  const PlayerPlaylistPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
